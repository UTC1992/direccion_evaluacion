@model direccion_evaluacion.Models.Modelos

@{
    ViewBag.Title = "Edit";
}
<div class="row">
    <nav class="navbar-inverse">

        <center>
            <h2> <i class="fa fa-user"> </i> <b>Administración de Fichas</b></h2>
        </center>

    </nav>
</div>

<h2>Editar item</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.Item.id)

        <div class="form-group">
            @Html.LabelFor(model => model.Item.nombre, "Nombre", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.Item.nombre, "Nombre", new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Item.nombre, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            @Html.LabelFor(model => model.Item.Subtitulo, "Subtitulo", htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                <select name="subtituloId">
                    <option>@Model.Item.Subtitulo.nombre</option>
                    @foreach (var item in Model.ObjSubtitulo)
                    {
                        <option value="@item.id">@item.nombre</option>
                    }
                </select>
            </div>
        </div>


        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Guardar" class="btn btn-success" />
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Regresar a items", "Index", null, new { @class = "btn btn-danger" })
</div>
